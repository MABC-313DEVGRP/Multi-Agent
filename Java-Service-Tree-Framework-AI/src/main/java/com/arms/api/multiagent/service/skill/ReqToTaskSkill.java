package com.arms.api.multiagent.service.skill;

import lombok.extern.slf4j.Slf4j;
import org.springframework.ai.tool.annotation.Tool;
import org.springframework.stereotype.Component;

@Slf4j
@Component
public class ReqToTaskSkill {

    public static final String NAME = "req_to_task";

    private static final String INSTRUCTIONS = """
            # Req To Task — 요구사항 착수 변환 방법론

            pm-expert의 요구사항 착수 변환 역량을 강화하는 스킬.
            요구사항정의서 행이나 요구사항 문장을 개발 착수용 작업 지시서로 바꾼다.
            해석을 고정된 규칙으로 대신하고, 규칙으로도 메울 수 없는 빈칸을 착수 전에 드러낸다.

            ## 대상 에이전트

            - **pm-expert** — 요구사항을 착수 지시서로 변환하고 완료 조건을 판정한다

            ## 1. 설계 원칙 — 지어내지 않는다

            | 상황 | 하지 않는 것 | 하는 것 |
            |---|---|---|
            | 완료 조건이 없다 | 그럴듯하게 생성 | `❓확인 필요`로 표시하고 질문 |
            | 완료 조건이 모호하다 | 그대로 옮김 | 측정 가능한 문장으로 바꿀 안을 제시하고 확인 요청 |
            | 원문에 없는 기술 스택·수치 | 상식으로 보충 | 쓰지 않음 |
            | 요구사항 2건이 한 행에 섞임 | 하나로 뭉갬 | 분할을 제안 |

            추론한 것은 반드시 `(추정)`으로 표시한다. 원문에 있는 것과 판단한 것이 구분되지 않으면 실패다.

            ## 2. 입력 받기

            엑셀 행 붙여넣기(탭 구분), CSV·마크다운 표, `요구사항명: ... / 상세: ...` 평문, 요구사항 문장 한 줄, 여러 건 동시를 모두 받는다.
            어떤 형태든 먼저 필드 역할부터 인식한다.

            - 요구사항 없이 요청만 오면: "요구사항정의서 행이나 요구사항 문장을 붙여넣어 주세요. 엑셀 행 복붙, CSV, 그냥 문장 모두 됩니다." 라고 답하고,
              "붙여넣을 자료가 마땅치 않으시면 **샘플로 보여줘** 라고 해주세요." 를 함께 안내한다.
            - "샘플로 보여줘" 요청이면 §8 예시 입력을 그대로 써서 전 과정을 수행한다. 새 예시를 만들지 않는다.
              마지막은 반드시 "이건 예시 데이터입니다. **실제 자료를 넣어 보세요** — 형식이 달라도 됩니다." 로 닫는다.
            - 채팅 입력창의 📎 로 엑셀(.xlsx)·CSV 파일을 올릴 수 있다. 파일로 오면 사용자 원문에 [첨부 파일] 과 [첨부 행] 이 붙어 온다.
              - 답변 첫 줄에 어느 시트를 썼는지 밝힌다. 파일에 시트가 여럿이면 다른 시트 이름도 함께 적는다.
              - [첨부 행] 의 "컬럼명: 값" 이 원문이다. 행 경계는 "--- n번째 행 ---" 로 이미 나뉘어 있으니 다시 세지 않는다. 컬럼명에 STEP 1 역할 인식을 적용한다.
              - 전달 범위가 "목록만" 이면 변환하지 않는다. 목록과 총 건수를 보여주고 전체를 변환할지 특정 건만 할지 묻는다. 답할 때 "전체" 또는 요구사항 ID 를 적어 달라고 안내한다.
              - [첨부 행] 이 비었거나 값이 깨져 읽을 수 없으면 추측하지 않고 해당 행을 붙여넣어 달라고 한다.
              - 변환을 마치면 맨 끝에 한 줄로, 엑셀로 받으려면 답변 아래 [엑셀로 받기] 를 누르거나 "엑셀로 줘" 라고 입력하라고 안내한다. 파일은 네가 만들지 않는다.
            - 파일 없이 붙여넣은 요구사항을 엑셀로 달라고 하면, 원본 컬럼 구성으로 받으려면 📎 로 요구사항정의서 파일을 올려 달라고 안내한다.

            ## 3. STEP 1 — 필드 역할 인식 (양식 비종속)

            컬럼명이 아니라 역할로 매칭한다.

            | 역할 | 실제로 쓰이는 컬럼명 |
            |---|---|
            | 식별자 | 요구사항 ID, ID, No, 번호, 항목번호 |
            | 분류 | 대/중/소분류, 구분, 모듈, 기능분류, 카테고리 |
            | 이름 | 요구사항명, 기능명, 제목, 요구사항 |
            | 내용 | 상세 내용, 상세, 설명, 내용, 요구사항 상세 |
            | 채택 | 수용 여부, 채택, 반영 여부, 검토 결과 |
            | 중요도 | 우선순위, 중요도, 등급, Priority |
            | 상태 | 진행 현황, 상태, 진행 상태, Status |
            | 담당 | 담당자, 담당, 개발자, Assignee |
            | 요청 | 요청자, 요청 부서, 고객 |
            | 기타 | 비고, 특이사항, Remark |

            `상세 내용` 안의 블록도 역할로 매칭한다.

            | 역할 | 실제 블록명 |
            |---|---|
            | 배경 | `[개요]` `[배경]` `[목적]` |
            | 작업 범위 | `[상세 기능 요구사항]` `[기능]` `[요구사항 상세]` `[입력/출력]` `[I/O]` |
            | 제약 | `[제약 조건]` `[제약사항]` `[비기능 요구사항]` |
            | 완료 조건 | `[검증 기준]` `[인수 조건]` `[완료 조건]` `[테스트 기준]` |

            블록이 없는 한 문단 서술이면 문장 단위로 역할을 추정한다. 금지·불가·말 것 → 제약. ~되어야 한다·~확인된다 → 완료 조건. 나머지 → 작업 범위. 추정한 항목에는 전부 `(추정)`을 붙인다.

            ## 4. STEP 2 — 변환 규칙

            - **제목**: 명사구 40자 이내. 동사형·문장형이면 명사구로 정규화한다.
            - **배경**: 왜 필요한지 1~2문장. 원문에 없으면 "원문에 없음". 만들지 않는다.
            - **작업 범위**: 체크 가능한 단위로 쪼갠다. 한 항목에 동작이 2개 이상이면 나눈다.
            - **완료 조건** (핵심 산출물): 원문의 검증·인수 기준을 판정 가능한 문장으로 옮기고 `✅` 측정 가능 / `⚠️` 모호 를 붙인다.
              `⚠️`에는 반드시 측정 가능하게 바꾼 대안 문장과 확인 질문을 같이 낸다.

            | 모호 표현 | 되물을 것 |
            |---|---|
            | 빠르게, 신속히 | 몇 초·몇 ms 이내인가 |
            | 안정적으로, 문제없이 | 허용 실패율·가동률이 얼마인가 |
            | 편리하게, 직관적으로 | 어떤 동작이 몇 단계로 끝나야 하는가 |
            | 실시간으로 | 허용 지연이 얼마인가 |
            | 대량의, 다수의 | 몇 건인가 |
            | 적절히, 유연하게 | 판단 기준이 무엇인가 |
            | 최적화, 개선 | 현재 값과 목표 값이 얼마인가 |
            | 사용자 친화적 | 어떤 지표로 측정하는가 |
            | ~등, 기타 | 목록을 닫을 수 있는가 |

            - **제약**: 금지 항목만 남긴다. 원문 제약 블록을 셋으로 나눈다.
              금지·하지 말 것 → 제약 / 달성해야 할 품질 → 완료 조건으로 올림 / 이미 정해진 환경 → 전제
            - **전제**: 이미 확정되어 논의 대상이 아닌 환경.
            - **산출물**: 상태 필드에서 유도한다. 상태가 없으면 비우고 확인 질문으로 돌린다.
              기획중·분석중 → 분석·설계 문서 / 설계중 → 상세 설계·스키마 / 개발중 → 소스 코드 / 테스트중·검증중 → 테스트 케이스·결과
            - **우선순위 · 분류**: 원문 표기를 그대로 옮기고 임의로 환산하지 않는다. 원문에 없으면 줄을 통째로 생략한다.
            - **개인정보 제거**: 담당자·요청자의 실명은 역할로 치환하고 이름을 출력하지 않는다.
              `DEV: 홍길동 / SE: 김철수` → `개발 담당, 시스템 담당`.

            ## 5. STEP 3 — 결손 진단

            착수를 막는 빈칸을 표시한다. 없으면 "결손 없음 — 이대로 착수 가능합니다" 라고 명시한다.

            | 표시 | 의미 |
            |---|---|
            | `❓` | 정보가 없다. 지어내지 않고 물어본다 |
            | `⚠️` | 있지만 판정 불가능하다. 대안 문장을 제시한다 |
            | `🔀` | 한 건에 요구사항이 2개 이상 섞였다. 분할을 제안한다 |
            | `🚫` | 변환하면 안 되는 행이다 (미수용·보류 등) |
            | `📌` | 원문을 손댄 곳이다 (실명 제거, 제약→완료 조건 이동 등). 되돌릴 수 있도록 반드시 남긴다 |

            ## 6. STEP 4 — 출력 전 자가 점검

            아래를 통과하지 못하면 출력하지 않고 고친다.
            - 완료 조건이 전부 `✅` 또는 `⚠️`로 판정되었는가
            - `제약`에 긍정문(해야 할 일)이 섞이지 않았는가
            - 원문에 없는 사실·수치·기술명을 추가하지 않았는가
            - 추론한 항목에 `(추정)`이 붙어 있는가
            - 실명이 남아 있지 않은가
            - 제목이 명사구인가

            ## 7. STEP 5 — 확인 질문

            변환 결과를 먼저 내고, 그다음에 묻는다. 질문 때문에 산출물을 미루지 않는다.
            - 질문은 최대 3개. 착수를 막는 것부터 묻는다 (`🔀`와 완료 조건 결손 우선).
            - `⚠️` 질문은 제시한 대안 문장을 선택지로 보여준다. 선택지 끝에 "모르겠다 / 나중에 정하겠다" 와 "직접 쓰겠다" 를 반드시 넣는다.
            - 이 대화는 이전 메시지를 기억하지 못한다. 질문 끝에 "답하실 때는 요구사항 원문과 답을 함께 붙여 주세요." 를 덧붙인다.
            - 원문과 답이 함께 오면 답을 반영해 다시 변환하고, 바뀐 항목에 `(답변 반영)`을 붙인다.
            - 답만 오고 원문이 없으면 변환하지 않고 원문을 함께 보내 달라고 요청한다.
            - [첨부 행] 이 붙어 온 경우에는 원문을 함께 붙여 달라는 안내를 하지 않는다. 화면이 원문을 다시 보낸다. 답만 와도 [첨부 행] 을 원문으로 보고 반영한다.

            ## 8. 출력 형식 (마크다운)

            각 건을 아래 형식으로 출력한다. 줄 사이에는 빈 줄을 두고, 목록은 `-` 나 `1.` 로 쓴다. 코드 블록과 JSON은 쓰지 않는다.

            ### [요구사항 ID] 제목

            **■ 배경**
            (1~2문장. 없으면 "원문에 없음")

            **■ 작업 범위**
            1. ...

            **■ 완료 조건**
            - ✅ (측정 가능한 판정 기준)
            - ⚠️ (모호) "원문 표현" → 대안: (측정 가능하게 고친 문장)

            **■ 제약 (하지 말 것)**
            - ...

            **■ 전제**
            - ...

            **■ 산출물**
            ...

            **■ 우선순위 / 분류**
            (원문에 있을 때만. 없으면 이 항목 자체를 생략)

            #### 결손 진단
            - ❓ ...

            #### 확인 질문
            1. ...

            여러 건이면 각 건을 출력한 뒤 마지막에 요약한다.

            #### 요약 (총 N건)
            - 착수 가능: N건
            - 확인 필요: N건 → [ID 목록]
            - 분할 권장: N건 → [ID 목록]
            - 변환 제외: N건 → [ID 목록]

            ## 9. 예외 처리

            | 입력 상황 | 처리 |
            |---|---|
            | 완료 조건 블록이 없다 | 지어내지 않는다. `❓완료 조건 없음 — 이 요구사항이 끝났다고 판단할 기준은 무엇입니까?` |
            | 완료 조건이 모호하다 | `⚠️` + 모호 표현 사전으로 대안 제시 |
            | 상세 내용이 블록 없는 한 문단 | 문장 단위 역할 추정, 전부 `(추정)` 표시 |
            | 컬럼명이 표에 없는 이름 | 내용을 보고 역할 추정. 판단 불가면 `❓` |
            | 컬럼 헤더 없이 값만 붙여넣음 | 값의 형태로 추정 (`REQ-`로 시작→ID, `High/Medium/Low`→중요도). 애매하면 되물음 |
            | 한 행에 요구사항 2건 이상 | `🔀` 분할 제안 + 분할안 제시. 원본도 함께 변환해 둔다 |
            | 채택 필드가 미수용·보류·검토중 | `🚫` 변환하지 않는다. `채택 상태가 '보류'입니다. 확정 후 진행하십시오.` |
            | 상태 필드가 완료·취소 | 변환하되 `이미 완료/취소된 건입니다` 경고를 먼저 낸다 |
            | 요구사항명만 있고 상세가 비었다 | 제목만 변환. 나머지는 전부 `❓` |
            | 상세만 있고 이름이 없다 | 내용에서 명사구 제목을 만들고 `(추정)` 표시 |
            | 엑셀 복붙 시 한 행이 여러 줄로 쪼개짐 (셀 안 줄바꿈) | 줄 수로 행을 세지 않는다. 식별자 패턴(`REQ-`, 연번)이 줄 맨 앞에 나오는 지점을 행 경계로 삼고, 그 사이 줄은 직전 행에 붙인다. 식별자가 없으면 `[개요]` 등 블록 머리표가 다시 나오는 지점을 경계로 본다 |
            | 행 경계를 판정할 수 없다 | 추측해서 쪼개지 않는다. `N건으로 보입니다. 맞습니까?` 확인 후 진행 |
            | 셀이 중간에 잘려 문장이 끊김 | `❓ 내용이 중간에 끊겼습니다. 잘린 부분을 확인해 주세요.` 끊긴 부분은 변환하지 않는다 |
            | 6건 이상 붙여넣음 | 먼저 목록과 건수를 요약해 보여주고, 전체 변환할지 특정 건만 할지 확인 |
            | 빈 행·머리글만 | 데이터 행이 없다고 알린다 |
            | 요구사항이 아닌 입력 (잡담·코드 등) | 범위를 한 줄로 안내하고 올바른 입력 형태를 제시 |
            | 한국어가 아닌 요구사항 | 원문 언어를 유지해 변환한다 |
            | 사용자가 확인 질문에 답하지 않았다 | 이미 낸 산출물이 최종본이다. 미답 항목은 `❓`로 남긴다 |

            ## 10. 예시 ("샘플로 보여줘" 입력)

            REQ-F-014	주문	결제	취소	주문 취소 처리	[개요]
            결제 완료된 주문에 대해 사용자가 취소를 요청하면 결제 취소와 재고 복원을 함께 처리한다.
            [상세 기능 요구사항]
            1) 취소 가능 여부 판정 후 결제사 취소 API 호출
            2) 취소 성공 시 재고 복원 및 주문 상태 변경
            [제약 조건]
            - 배송 시작 이후 건은 취소 불가
            - 결제사 API는 하루 1만 건 한도, 초과 시 지연 처리
            - 취소 이력은 삭제하지 않고 보존
            [검증 기준]
            - 취소 요청 시 결제 취소와 재고 복원이 모두 반영됨
            - 시스템이 안정적으로 동작함
            	기획팀 홍길동	수용	High	DEV: 김철수	개발중	-

            이 입력에서 일어나야 할 일:
            - 검증 기준 2개 중 "시스템이 안정적으로 동작함"은 `⚠️`. 대안 예: "결제사 API 호출 실패 시 재고가 복원되지 않고, 주문 상태도 변경되지 않는다 (부분 반영 없음)"
            - 제약의 "취소 이력은 삭제하지 않고 보존"은 달성 목표라 완료 조건으로도 올린다. "결제사 API 일 1만 건 한도"는 전제다.
            - `❓` 취소 가능 여부 판정 기준(배송 시작 외 조건), `❓` 결제사 API 실패·한도 초과 시 사용자에게 보여줄 것
            - `📌` 요청자·담당자는 '기획 담당 / 개발 담당'으로 치환
            - 산출물은 소스 코드 (상태: 개발중), 우선순위 / 분류는 High / 주문 > 결제 > 취소
            """;

    @Tool(name = NAME, description = "요구사항정의서 행이나 요구사항 문장을 개발 착수용 작업 지시서로 바꾸는 절차를 돌려준다. "
            + "'요구사항 정리해줘', '이 요구사항 티켓으로 만들어줘', '요구사항정의서 변환', '완료 조건 뽑아줘', '요구사항 검토해줘', "
            + "확인 질문에 대한 답 반영, '샘플로 보여줘' 에 사용한다. 변환과 함께 완료 조건이 측정 가능한지 판정하고, "
            + "빠진 정보는 지어내지 않고 확인 질문으로 되돌린다. 요구사항을 새로 발굴·작성하거나 이슈 트래커에 등록하는 일은 범위가 아니다.")
    public String reqToTask() {
        log.info("ReqToTaskSkill :: 스킬 호출");
        return INSTRUCTIONS;
    }
}
